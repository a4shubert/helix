# helix-core

Core analytics library for the Helix platform.

## Minimal example

```python
from helix_core import mark_to_market

market_value = mark_to_market(quantity=1_000_000, price=99.25)
print(market_value)
```

A Jupyter example is available at `notebooks/helix_core_demo.ipynb`.
